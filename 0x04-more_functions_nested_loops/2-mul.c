#include "main.h"

/**
 * _multiplication - multiplies two integers
 * @a: a is an integer
 * @b: b is an integer
 * @c: c is an integer
 * @mul: is an integer
 * Return: Always 0
 */
int mul(int a, int b)
{
	int a = 4;
	int b = 3;
	int c;

	if (a * b > 0)
	{
		_putchar(c);
	}
	_putchar('\n');
}
